package model;

public class Customer extends User {
    public Customer(String login, String password, String fullname, String cpf, String phone) {
        super(login, password, fullname, cpf, phone);
    }
}
